% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration.R
\name{xrf_spectra_from_counts}
\alias{xrf_spectra_from_counts}
\title{Build a spectra object from a raw counts vector and an energy calibration}
\usage{
xrf_spectra_from_counts(
  counts,
  gain,
  zero = 0,
  quad = 0,
  channel = seq_along(counts) - 1L,
  livetime = NA_real_,
  ...,
  .validate = TRUE
)
}
\arguments{
\item{counts}{Integer/numeric vector of counts per channel.}

\item{gain, zero, quad}{Energy calibration (keV), passed to \link{xrf_calibrate_energy}.}

\item{channel}{Channel indices for \code{counts}. Default \code{seq_along(counts) - 1} (0-based).}

\item{livetime}{Acquisition live time in seconds. If finite and > 0, a \code{cps} column is
added and stored alongside as the \code{LiveTime} metadata column; otherwise \code{cps} is set
equal to \code{counts}.}

\item{...}{Additional metadata columns (e.g. \code{SampleIdent = "std-1"}), passed to
\link{xrf_spectra}.}

\item{.validate}{Passed to \link{xrf_spectra}.}
}
\value{
A spectra tibble with one row.
}
\description{
A vendor-neutral entry point for spectra that arrive as counts-per-channel plus a calibration
(the usual SEM-EDS / generic-MCA case), rather than through a vendor-specific reader such as
\link{read_xrf_panalytical}. It applies \link{xrf_calibrate_energy} to build the \code{energy_kev}
axis and, when \code{livetime} is supplied, a \code{cps} column (\code{counts / livetime}); the
raw \code{counts} and the live time are retained so that Poisson-weighted deconvolution
(\code{weighting = "poisson"}) can use them.
}
\examples{
cts <- rpois(2048, 5)
xrf_spectra_from_counts(cts, gain = 0.02, zero = 0, livetime = 60, SampleIdent = "demo")

}
