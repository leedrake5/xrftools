% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detector.R
\name{xrf_tube}
\alias{xrf_tube}
\title{X-ray tube description (reserved)}
\usage{
xrf_tube(
  anode = NA_character_,
  kv = NA_real_,
  ma = NA_real_,
  filter = NA_character_,
  ...
)
}
\arguments{
\item{anode}{Anode element symbol (e.g. "Rh", "W", "Ag", "Mo", "Au").}

\item{kv}{Tube accelerating voltage (kV); the bremsstrahlung endpoint energy in keV.}

\item{ma}{Tube current (mA), optional.}

\item{filter}{Primary beam filter description, optional.}

\item{...}{Additional named fields to carry.}
}
\value{
An \code{xrf_tube} object (a classed list).
}
\description{
A lightweight container describing the excitation source (X-ray tube). It is carried through the
analysis so that a single object can hold all instrument conditions, but it is \strong{not yet
consumed by the deconvolution fit} -- it is reserved for the forthcoming tube-spectrum,
Rayleigh/Compton scatter, and fundamental-parameters work. Supplying it today is harmless and
future-proofs your scripts.
}
\examples{
xrf_tube("Rh", kv = 40)

}
